/* Lab Exercise 2
	Add two long positive integers represented using circular doubly linked list with
	header node.
*/

#include <stdio.h>
#include <stdlib.h>

typedef struct node {
	int digit;
	struct node* next;
	struct node* prev;
} node;

int main(void) {
	

}